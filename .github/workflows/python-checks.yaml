name: Python CI

on:
  push:
    paths:
      - '**.py'
      - '**.lock'
      - '**.toml'
      - '**.yaml'
      - '**.yml'
      - '**.ini'
    branches: [ main ]
  pull_request:
    paths:
      - '**.py'
      - '**.lock'
      - '**.toml'
      - '**.yaml'
      - '**.yml'
      - '**.ini'
    branches: [ main ]

jobs:
  ci:
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9"]
        poetry-version: ["1.8.3"]
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Poetry cache
        uses: actions/cache@v2
        with:
          path: ~/.local
          key: poetry-1.8.3-0
      - name: Poetry install
        uses: snok/install-poetry@v1
        with:
          version: 1.8.3
          virtualenvs-create: true
          virtualenvs-in-project: true
      - name: cache deps
        id: cache-deps
        uses: actions/cache@v2
        with:
          path: .venv
          key: pydeps-${{ hashFiles('**/poetry.lock') }}
      - run: poetry install --no-interaction --no-root
        if: steps.cache-deps.outputs.cache-hit != 'true'
      - run: poetry install --no-interaction
      - name: mypy
        run: poetry run mypy -p boolean_circuit_tool
      - name: flake8
        run: poetry run flake8
      - name: pytest
        run: poetry run pytest
      - name: usort
        run: poetry run usort check boolean_circuit_tool
      - name: docformatter
        run: poetry run docformatter --check --diff boolean_circuit_tool/
      - name: black
        run: poetry run black --check --diff boolean_circuit_tool/ tests/
